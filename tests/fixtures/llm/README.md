# Real LLM semantic fixtures

This directory contains fixed Chinese semantic-quality fixtures for PromptCut Studio.

- No real user media is stored here.
- No online LLM provider, API key, token, production URL, or presigned URL is required.
- Codex CLI runs, when explicitly enabled, must receive prompt input through stdin in a read-only, no-network sandbox.

